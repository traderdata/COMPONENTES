namespace ModulusFE.Models
{
    internal partial class SeriesModelBase
    {
        public int StartIndex { get; private set; }
        public int EndIndex { get; private set; }
        public bool IsOscillator { get; private set; }
        public SeriesTypeEnum SeriesType { get; private set; }
        public Series Series { get; private set; }

        public SeriesModelBase(int startIndex, int endIndex, bool isOscillator, SeriesTypeEnum seriesType, Series series)
        {
            StartIndex = startIndex;
            EndIndex = endIndex;
            IsOscillator = isOscillator;
            SeriesType = seriesType;
            Series = series;
        }


    }
}
